# Core UI
PyQt6>=6.6.0

# Spotify API
spotipy>=2.23.0

# Audio download
yt-dlp>=2024.1.0

# Vocal isolation (inference-only Demucs)
demucs-infer>=4.1.0

# Pitch detection
torchcrepe>=0.0.22

# PyTorch (CUDA optional - install torch+cu121 manually for GPU)
torch>=2.1.0
torchaudio>=2.1.0

# Audio I/O
sounddevice>=0.4.6
PyAudioWPatch>=0.2.12; sys_platform == "win32"

# Audio processing
numpy>=1.24.0
scipy>=1.11.0
librosa>=0.10.1
soundfile>=0.12.1

# Environment / config
python-dotenv>=1.0.0

# Optional: Silero VAD (voice activity gate for the vocal monitor) is
# fetched on first use via torch.hub; no separate install required.
